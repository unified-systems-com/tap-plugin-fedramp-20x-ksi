{
  "slug": "HAS_EVIDENCE",
  "name": "Has Evidence",
  "description": "A finding has supporting evidence. The support_kind property classifies the verdict the evidence supports (passing, violation, informational). Multiple HAS_EVIDENCE edges per finding are expected; the finding's effective verdict is aggregated from its evidence.",
  "sources": ["finding"],
  "targets": ["evidence"],
  "property_schema": {
    "type": "object",
    "required": ["support_kind"],
    "additionalProperties": false,
    "properties": {
      "support_kind": {
        "type": "string",
        "enum": ["passing", "violation", "informational"]
      },
      "note": {"type": "string"}
    }
  },
  "default_dimensions": {
    "compliance": "fedramp-20x"
  }
}
